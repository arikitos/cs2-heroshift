name: Refactor Player Event Routing

on:
  pull_request:
    branches: [main]
    types: [edited]

permissions:
  contents: write
  pull-requests: write

jobs:
  player-routing:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'refactor' &&
      github.event.pull_request.user.login == 'arikitos' &&
      contains(github.event.pull_request.body || '', '<!-- dispatch:player-routing -->')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout refactor
        uses: actions/checkout@v4
        with:
          ref: refactor
          fetch-depth: 0

      - name: Apply player event routing patch
        shell: bash
        run: |
          base64 --decode tools/refactor-dispatch/player-simple-routing.patch.gz.b64 | gzip --decompress > player-routing.patch
          git apply --check player-routing.patch
          git apply player-routing.patch
          rm player-routing.patch tools/refactor-dispatch/player-simple-routing.patch.gz.b64
          python3 - <<'PY'
          from pathlib import Path
          path = Path('CHANGELOG.md')
          text = path.read_text(encoding='utf-8')
          entry = '- Route player, weapon, grenade, hurt-suppression, death, disconnect and skill-use callbacks through typed dispatcher methods while retaining the specialized tick and damage-order paths for separate migration.'
          if entry not in text:
              text = text.replace('### Changed\n\n', f'### Changed\n\n{entry}\n\n', 1)
          path.write_text(text, encoding='utf-8')
          PY
          git diff --check

      - name: Validate player routing
        shell: bash
        run: |
          file='HeroShift - SRC Files/src/player/PlayerEvents.cs'
          grep -Fq 'DispatchPlayerHurtPre(' "$file"
          grep -Fq 'DispatchPlayerDisconnect(' "$file"
          grep -Fq 'DispatchPlayerDeath(' "$file"
          grep -Fq 'InvokeUseSkill(' "$file"
          grep -Fq 'InvokeDisableSkill(' "$file"
          grep -Fq 'DispatchBulletImpact(' "$file"
          if grep -Fq 'AskSkillSuppressesHit' "$file"; then exit 1; fi

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore "HeroShift - SRC Files/HeroShift.sln"

      - name: Test
        run: dotnet test "HeroShift - SRC Files/HeroShift.sln" -c Release --no-restore

      - name: Build
        run: dotnet build "HeroShift - SRC Files/HeroShift.sln" -c Release --no-restore

      - name: Commit player routing
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'refactor: route player events through typed dispatcher'
          git push origin HEAD:refactor

      - name: Clear player routing request
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const body = context.payload.pull_request.body || '';
            const cleaned = body.replace('<!-- dispatch:player-routing -->', '<!-- dispatch:player-routing-complete -->');
            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              body: cleaned,
            });
